import { Box } from "@mui/material";

const NotFoundPage = () => {
    return <div>
        <Box sx={{ height: '100%' }}>
            Not Found
        </Box>
    </div>
}

export default NotFoundPage;